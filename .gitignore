/target

# Vendored FFmpeg system libs produced by player/scripts/build-ffmpeg.sh.
# Local artefacts only — every dev/runner builds their own per platform.
# See player/FFMPEG_BUILD.md for what goes here.
player/vendor/

# Local test recordings / extracted frames (kept out of repo)
captures/
frames*/
phone_*/
play*.mp4
playback*.mp4
phone*.mp4
video_*.mp4
android.log
durations.txt

# Local run logs captured from app stdout/stderr
stdout.log
stderr.log

# Stale build output location (real output goes to app-android/android/app/src/main/jniLibs)
app-android/src/main/jniLibs/

# iOS Simulator build output (RustPlayer.app bundle assembled by build_sim.sh)
app-ios/ios/build/
